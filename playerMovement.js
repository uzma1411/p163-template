AFRAME.registerComponent("player-movement", {
  init: function () {
    this.walk();
  },
  walk: function () {
    window.addEventListener("keydown", (e) => {

      if (
        //check e.key is equal to ArrowUp or  ArrowRight etc
      ) {

        // use query selector and refer to sound2 id

        // call the playSound function for the entity
        
      }
    });
  },
});

